# Variabili d'ambiente per il backend U.S. Corbiolo Calcetto
# Copia questo file in .env e modifica i valori secondo la tua configurazione.

# Configurazione Database PostgreSQL
DB_USER=postgres
DB_HOST=localhost
DB_NAME=corbiolo_calcetto
DB_PASSWORD=tua_password_sicura
DB_PORT=5432

# Porta del server backend (opzionale, default 3000)
# PORT=3000

# API Key per le operazioni di scrittura (POST, PUT, DELETE)
# Se non impostata, verrà usata una chiave di default ('SUPER_SECRET_API_KEY') definita in server.js
# API_KEY=LA_TUA_CHIAVE_API_PERSONALE
